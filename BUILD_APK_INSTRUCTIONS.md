# Пошаговая инструкция: Создание APK в Android Studio

## ✅ Подготовка завершена

Веб-приложение собрано и синхронизировано с Android проектом.

## 📱 Создание APK в Android Studio

### Шаг 1: Дождитесь открытия Android Studio

Android Studio должен открыться автоматически. Если не открылся:
- Откройте Android Studio вручную
- Выберите **File** → **Open**
- Выберите папку `android` в проекте

### Шаг 2: Дождитесь синхронизации Gradle

После открытия проекта:
1. Android Studio начнет синхронизацию Gradle
2. Дождитесь завершения (внизу будет прогресс-бар)
3. Если есть ошибки, исправьте их перед сборкой

### Шаг 3: Соберите Debug APK (для тестирования)

**Самый простой способ:**

1. В меню Android Studio: **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
2. Дождитесь завершения сборки (внизу будет прогресс)
3. Когда появится уведомление **"APK(s) generated successfully"**
4. Нажмите **locate** в уведомлении
5. APK файл будет в: `android/app/build/outputs/apk/debug/app-debug.apk`

**Альтернативный способ через терминал:**

```bash
cd android
.\gradlew assembleDebug
```

APK будет в: `android/app/build/outputs/apk/debug/app-debug.apk`

### Шаг 4: Соберите Release APK (для публикации)

⚠️ **Важно:** Release APK требует подписи ключом.

1. В меню: **Build** → **Generate Signed Bundle / APK**
2. Выберите **APK** → **Next**
3. **Создайте новый ключ** (если нет):
   - Нажмите **Create new...**
   - Заполните данные:
     - **Key store path:** выберите место (например, `android/app/release-key.jks`)
     - **Password:** придумайте пароль (запомните!)
     - **Key alias:** например, `momrecipes`
     - **Key password:** пароль для ключа
     - **Validity:** 25 лет
     - **Certificate:** заполните данные (имя, организация и т.д.)
   - Нажмите **OK**
4. Выберите созданный ключ → введите пароли
5. Выберите **release** build variant → **Finish**
6. APK будет в: `android/app/build/outputs/apk/release/app-release.apk`

**⚠️ ВАЖНО:** Сохраните ключ подписи в безопасном месте! Без него вы не сможете обновлять приложение в Google Play.

## 🔧 Решение проблем

### Ошибка "SDK not found"
1. **File** → **Settings** → **Appearance & Behavior** → **System Settings** → **Android SDK**
2. Установите необходимые SDK компоненты (API 34, Build Tools и т.д.)

### Ошибка Gradle
1. **File** → **Sync Project with Gradle Files**
2. Или: **Build** → **Clean Project**, затем **Rebuild Project**

### APK не устанавливается
- Проверьте, что включена установка из неизвестных источников на устройстве
- Убедитесь, что APK подписан (release APK)
- Проверьте минимальную версию Android в `build.gradle` (сейчас: API 22)

## 📦 Размер APK

Ожидаемый размер:
- **Debug APK:** ~20-30 MB
- **Release APK:** ~10-20 MB (оптимизированный)

## 🚀 Установка APK на устройство

### Способ 1: Через USB (рекомендуется)

1. На Android устройстве:
   - Настройки → О телефоне → нажмите 7 раз на "Номер сборки" (включит режим разработчика)
   - Настройки → Для разработчиков → включите "Отладка по USB"
2. Подключите устройство к компьютеру USB кабелем
3. В Android Studio: **Run** → **Run 'app'**
4. Выберите ваше устройство
5. Приложение установится автоматически

### Способ 2: Через файл

1. Скопируйте APK файл на Android устройство (через USB, email, облако)
2. На устройстве откройте файловый менеджер
3. Найдите APK файл и откройте его
4. Если нужно, разрешите установку из неизвестных источников
5. Нажмите "Установить"

## 📝 Проверка перед сборкой

Убедитесь, что:
- ✅ `npm run build` выполнен успешно
- ✅ Папка `dist/` существует и содержит файлы
- ✅ `npm run cap:sync` выполнен успешно
- ✅ Android Studio открыт и проект загружен
- ✅ Gradle синхронизация завершена (без ошибок)

## 🎯 Быстрая команда (все сразу)

Если Android проект уже создан:

```bash
npm run build && npm run cap:sync && npm run cap:open:android
```

Затем в Android Studio: **Build** → **Build APK(s)**

---

**Готово!** После сборки APK будет готов к установке или публикации.
