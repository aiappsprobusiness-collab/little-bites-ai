# Speech-to-Text: Быстрый старт

## Что реализовано

✅ **Проверка разрешений** - автоматическая проверка и запрос доступа к микрофону  
✅ **Fallback механизмы** - автоматический переход на альтернативные методы  
✅ **Русский язык** - поддержка ru-RU из коробки  
✅ **Android Native** - структура для интеграции с Android SpeechRecognizer (работает офлайн)  
⚠️ **Web Speech API** - требует интернет-соединение почти всегда  

## Как использовать

### В веб-браузере

1. Откройте приложение в **Chrome**, **Edge** или **Safari**
2. Перейдите в чат
3. Нажмите кнопку микрофона 🎤
4. Разрешите доступ к микрофону (если запрошено)
5. Говорите на русском языке

### На Android устройстве

1. Убедитесь, что Android плагин настроен (см. `SPEECH_TO_TEXT_ANDROID_SETUP.md`)
2. Соберите и установите приложение
3. Откройте чат
4. Нажмите кнопку микрофона
5. Говорите на русском языке

## Решенные проблемы

### 1. Проверка разрешений ✅

- Автоматическая проверка доступа к микрофону
- Запрос разрешений с понятными сообщениями
- Обработка отказа в доступе

### 2. Fallback на альтернативные методы ✅

- Web Speech API (основной метод для веб)
- Android Native SpeechRecognizer (для Android)
- Автоматический выбор лучшего доступного метода

### 3. Русский язык ✅

- Язык установлен на `ru-RU` по умолчанию
- Поддержка в Web Speech API
- Поддержка в Android SpeechRecognizer

### 4. Офлайн режим ⚠️

- **Web Speech API**: почти всегда требует интернет-соединение. Экспериментальный офлайн-режим возможен только в очень узких сценариях (часть Chrome на Android с предзагруженными языковыми пакетами), но поведение нестабильно, особенно для русского языка. **Не рекомендуется полагаться на офлайн режим Web Speech API.**
- **Android Native**: работает полностью офлайн, но зависит от установленного сервиса распознавания (Google/производитель). При отсутствии или блокировке сервиса может не работать.
- Понятные сообщения об ошибках при отсутствии интернета

## Структура файлов

```
src/
  services/
    speechToTextEnhanced.ts    # Улучшенный сервис с проверкой разрешений
    speechToText.ts            # Старый сервис (оставлен для совместимости)
  components/
    chat/
      ChatInputPanel.tsx       # Обновлен для использования нового сервиса

android/
  app/src/main/java/com/momrecipes/app/
    SpeechRecognitionPlugin.java  # Нативный Android плагин
```

## Следующие шаги (опционально)

Для полностью офлайн распознавания можно добавить:

1. **Vosk** - офлайн библиотека распознавания речи
2. **Whisper** - локальное распознавание через Edge Functions
3. **Улучшенная обработка ошибок** - более детальная диагностика

## Тестирование

### Проверка работы

1. Откройте консоль браузера (F12)
2. Нажмите кнопку микрофона
3. Проверьте сообщения в консоли:
   - `Speech recognition started successfully` - успешный запуск
   - Сообщения об ошибках - если что-то не работает

### Типичные проблемы

**"Доступ к микрофону запрещен"**
- Разрешите доступ в настройках браузера
- Проверьте настройки сайта (иконка замка в адресной строке)

**"Проблема с сетью"**
- Web Speech API **почти всегда** требует интернет-соединение и доступ к серверам Google
- На Android Chrome Web Speech API сильно зависит от установленного сервиса распознавания (Google/производитель), при его отсутствии или блокировках часто не работает вообще
- Используйте Android Native для офлайн режима (требует настройки плагина)
- Или используйте VPN если доступ к Google заблокирован
- Или введите текст вручную (всегда работает)

**"Распознавание речи не поддерживается"**
- Убедитесь, что используете Chrome, Edge или Safari
- Проверьте, что сайт открыт по HTTPS или localhost

## Дополнительная информация

См. `SPEECH_TO_TEXT_ANDROID_SETUP.md` для детальной инструкции по настройке Android плагина.
