# Настройка GigaChat AI для MomRecipes

## Что такое GigaChat?

GigaChat - это российский AI-ассистент от Сбера, который интегрирован в приложение для:
- 📸 **Анализа фото продуктов** - распознавание продуктов на изображениях
- 👨‍🍳 **Генерации рецептов** - создание рецептов на основе продуктов с учетом возраста и аллергий
- 💡 **Рекомендаций** - персональные советы по питанию для детей

## Получение API ключа

1. Перейдите на https://developers.sber.ru/gigachat
2. Зарегистрируйтесь или войдите в аккаунт
3. Создайте новое приложение
4. Получите Client Secret Key
5. Конвертируйте ключ в Base64 формат (если требуется)

## Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# GigaChat API Configuration
VITE_GIGACHAT_CLIENT_SECRET_KEY=your_base64_encoded_key_here
VITE_GIGACHAT_IS_PERSONAL=true
```

### Как получить Base64 ключ

Если у вас есть обычный ключ, конвертируйте его в Base64:

**В браузере (консоль):**
```javascript
btoa('your_client_secret_key')
```

**В Node.js:**
```javascript
Buffer.from('your_client_secret_key').toString('base64')
```

**В PowerShell:**
```powershell
[Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes('your_client_secret_key'))
```

## Важно для безопасности

⚠️ **Для продакшена рекомендуется использовать backend proxy!**

Текущая реализация работает напрямую из браузера, что означает:
- API ключ будет виден в коде клиента
- Это подходит для разработки и тестирования
- Для продакшена создайте backend API, который будет делать запросы к GigaChat

### Пример backend proxy (Node.js/Express)

```javascript
// server.js
const express = require('express');
const { GigaChat } = require('gigachat-node');

const app = express();
app.use(express.json());

const gigachat = new GigaChat({
  clientSecretKey: process.env.GIGACHAT_CLIENT_SECRET_KEY,
  isPersonal: true,
});

app.post('/api/analyze-image', async (req, res) => {
  try {
    const { imageBase64 } = req.body;
    const result = await gigachat.analyzeImage(imageBase64);
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});

app.listen(3000);
```

## Использование

После настройки переменных окружения:

1. **Анализ фото продуктов:**
   - Откройте страницу "Сканировать"
   - Сфотографируйте или выберите фото продуктов
   - GigaChat автоматически распознает продукты

2. **Генерация рецептов:**
   - После анализа выберите нужные продукты
   - Нажмите "Создать рецепт с GigaChat"
   - Рецепт будет создан с учетом возраста ребенка и аллергий

3. **Рекомендации:**
   - На главной странице отображаются персональные рекомендации
   - Рекомендации обновляются автоматически

## Решение проблем

### Ошибка "GigaChat не настроен"

Убедитесь, что:
- Файл `.env` создан в корне проекта
- Переменная `VITE_GIGACHAT_CLIENT_SECRET_KEY` установлена
- Перезапустите dev сервер после изменения `.env`

### Ошибка аутентификации

- Проверьте правильность Base64 ключа
- Убедитесь, что ключ действителен
- Проверьте тип аккаунта (`VITE_GIGACHAT_IS_PERSONAL`)

### CORS ошибки

GigaChat API может блокировать запросы из браузера. В этом случае:
- Используйте backend proxy (рекомендуется)
- Или настройте CORS на стороне GigaChat (если доступно)

## Дополнительная информация

- [Документация GigaChat API](https://developers.sber.ru/gigachat/docs)
- [GigaChat Node.js библиотека](https://github.com/zloishavrin/gigachat-node)
