# Скопируйте в `.env` в корне проекта для локальной разработки.
# Сами файлы `.env` / `.env.local` в .gitignore — не коммитить.
# Подробности переменных: DEEPSEEK_SETUP.md, docs/dev/dev-notes.md

# ---------------------------------------------------------------------------
# Локальный журнал правок по VK-воронке (НЕ коммитить, не пушить в прод):
#   VK_FUNNEL_LOCAL_CHANGELOG.md  — в корне репозитория, в .gitignore
# Формат записей: docs/dev/vk-funnel-local-changelog.md
# ---------------------------------------------------------------------------
